import 'package:flutter/material.dart';

class CalaIcons {
  static const principalIcon = Icon(Icons.calendar_view_day);
  static const histIcon = Icon(Icons.calendar_today);
  static const catalogIcon = Icon(Icons.food_bank);
  static const progressIcon = Icon(Icons.assignment);
  static const objetivIcon = Icon(Icons.settings);
  static const salirIcon = Icon(Icons.exit_to_app);

  static const addIcon = Icon(Icons.add);
}
